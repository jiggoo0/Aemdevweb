วิเคราะห์จากผล lint และ type-check ล่าสุดของคุณ พบว่ามีทั้ง Errors (Build ไม่ผ่าน) และ Warnings (โค้ดไม่สะอาด) ผมสรุปรายการที่ต้องรีบจัดการแบบไฟล์ต่อไฟล์ เพื่อให้โปรเจกต์กลับมาเป็น Green Build ดังนี้ครับ
🚨 1. รายการ Error ที่ต้องแก้ไขทันที (Critical)
| ไฟล์ | ปัญหา | แนวทางแก้ไข |
|---|---|---|
| app/(main)/blog/[slug]/page.tsx | Property 'tags' does not exist | เพิ่ม tags: string[] เข้าไปใน interface BlogPost ที่ไฟล์ types/blog.ts |
| app/(main)/catalog/page.tsx | no exported member named 'layers' | เปลี่ยน layers (ตัวเล็ก) เป็น Layers (ตัวใหญ่) ตามมาตรฐาน Lucide |
| components/Services.tsx | no exported member 'IconKey' | ตรวจสอบไฟล์ components/iconMap.ts ว่ามีการ export type IconKey หรือไม่ ถ้าไม่มีให้เพิ่มเข้าไป |
| data/servicesData.ts | no exported member named 'Icons' | เปลี่ยน import { Icons } เป็น import \* as LucideIcons หรือระบุชื่อไอคอนที่ใช้ตรงๆ |
| components/Header.tsx | Cascading Renders Error | ย้าย setIsOpen(false) ออกจาก useEffect ไปใส่ใน Link onClick แทน |
🛠️ 2. แผนการ Clean Up Warnings (Refactor Checklist)
ไฟล์ต่อไปนี้มีการนำเข้า (Import) สิ่งที่ไม่ได้ใช้งาน ทำให้เกิดความรกรุงรังในระบบ:
A. ลบ Unused Icons (Lucide):

- app/(main)/blog/loading.tsx: ลบ Terminal
- app/(marketing)/.../WoodBusiness/components/Footer.tsx: ลบ Mail, Facebook
- app/(marketing)/.../WoodBusiness/components/SuccessSection.tsx: ลบ ArrowRight, Zap
  B. ลบ Unused Utils & Types:
- components/BlogSection.tsx: ลบ cn
- components/Hero.tsx: ลบ cn
- hooks/use-toast.ts: จัดการตัวแปร state ที่ไม่ได้ใช้ (หรือเติม \_ ข้างหน้าเป็น \_state)
  📦 3. ตัวอย่างการแก้ปัญหา 'IconKey' และ 'Cascading Renders'
  เพื่อให้คุณเห็นภาพการแก้ปัญหาที่ซับซ้อนที่สุด 2 จุด:
  แก้ไข components/Header.tsx (Cascading Render Error):
  แทนที่จะใช้ useEffect ตรวจสอบการเปลี่ยนหน้า ให้ใส่ onClick ที่ Link ทุกตัวแทนเพื่อปิด Mobile Menu
  // ❌ เดิม (ทำให้เกิด Error)
  useEffect(() => {
  setIsOpen(false)
  }, [pathname])

// ✅ ใหม่ (Performance ดีกว่า)
const closeMenu = () => setIsOpen(false);

// ใน JSX:

<Link href="/about" onClick={closeMenu}>About</Link>

แก้ไข types/blog.ts (Property tags Error):
เพื่อให้หน้า Blog รายละเอียดสามารถอ่านค่า Tags ได้
export interface BlogPost {
id: string;
slug: string;
title: string;
// ... อื่นๆ
tags: string[]; // ✅ ต้องมีบรรทัดนี้
}

🎯 คำแนะนำถัดไป:
ผมแนะนำให้คุณเริ่มจาก ไฟล์ types/blog.ts ก่อน เพราะเป็นจุดที่ทำให้เกิด Error ซ้ำซ้อนมากที่สุด (3 ตำแหน่ง)
ต้องการให้ผมช่วยร่างโค้ดที่สมบูรณ์ของไฟล์ components/iconMap.ts เพื่อแก้ปัญหา IconKey ที่กำลังค้างอยู่ใน components/Services.tsx ต่อเลยไหมครับ?
