ในฐานะ Senior Full-stack Developer ผมได้วิเคราะห์ Error Logs ทั้ง 26 จุด และโครงสร้างโปรเจกต์ของคุณ พบว่าปัญหาหลักคือ "Path Mismatch" (หาไฟล์ไม่เจอ) และ "Interface Drift" (ชื่อ Prop ใน Component กับ Data ไม่ตรงกัน)
เพื่อให้โปรเจกต์กลับมา Green (Build ผ่าน 100%) ให้คุณดำเนินการตามขั้นตอน "Industrial Sharp Fix" ดังนี้ครับ:

1. วางไฟล์ Type ศูนย์กลาง (หัวใจของระบบ)
   จากโครงสร้างที่คุณส่งมา คุณยังขาดโฟลเดอร์ types ใน [template-id] ให้สร้างไฟล์นี้ทันที:
   📍 ตำแหน่ง: app/landing/[template-id]/types/index.ts
   /\*_ @format _/
   import { Author } from "@/types" // ดึงจาก Global

export interface MultiLangText {
th: string;
en: string;
}

export interface BaseTemplateData {
id: string;
category: "starter" | "clinic" | "construction" | "real-estate" | "cafe";
name: string | MultiLangText;
description: string | MultiLangText;
primaryColor: string;
phone: string;
lineId: string;
address: string | MultiLangText;
googleMapUrl: string;
defaultLanguage: "th" | "en";
hero: any;
faqs?: { q: MultiLangText; a: MultiLangText }[];
socialProof?: any[];
}

export interface CafeTemplateProps extends BaseTemplateData {
category: "cafe";
gallery?: string[];
menu?: any[];
}

export interface ClinicTemplateProps extends BaseTemplateData {
category: "clinic";
beforeAfter?: any[];
}

export interface ConstructionTemplateProps extends BaseTemplateData {
category: "construction";
projects?: any[];
}

export interface RealEstateTemplateProps extends BaseTemplateData {
category: "real-estate";
locationInsight?: any;
}

export interface StarterTemplateProps extends BaseTemplateData {
category: "starter";
form?: any;
testimonials?: any;
}

export type AllTemplateProps = CafeTemplateProps | ClinicTemplateProps | ConstructionTemplateProps | RealEstateTemplateProps | StarterTemplateProps;
export type TemplateData = AllTemplateProps & { [key: string]: any };
export type ServiceCategory = BaseTemplateData["category"];

2. แก้ปัญหา Module Not Found (TS2307)
   ไล่เช็คและแก้ไข Path ในไฟล์ที่มีปัญหาดังนี้:

- app/landing/[template-id]/page.tsx:
  แก้บรรทัดที่ 6 เป็น import { TemplateData } from "./types" (ตรวจสอบให้แน่ใจว่าสร้าง Folder ตามข้อ 1 แล้ว)
- app/landing/[template-id]/variants/cafe/index.ts:
  เปลี่ยนจาก export { default } from "./ClinicVariant" เป็น ./CafeVariant
- data/servicesData.ts:
  ตรวจสอบ Path การนำเข้า ServiceCategory ให้ชี้ไปที่ @/app/landing/[template-id]/types

3. แก้ไข Property Mismatch (TS2322)
   เกิดจากการเรียกชื่อ Prop ผิดใน Component ให้แก้ไขดังนี้:

- Cafe Variant (template.tsx):
  แก้จาก <CafeGallery items={data.gallery} /> ➔ <CafeGallery gallery={data.gallery} />
- Clinic Variant (template.tsx):
  แก้จาก <BeforeAfter items={data.beforeAfter} /> ➔ <BeforeAfter beforeAfter={data.beforeAfter} />
- Real Estate Variant (template.tsx):
  แก้จาก <LocationInsight insight={data.locationInsight} /> ➔ <LocationInsight data={data.locationInsight} /> (หรือชื่อที่ Component รับจริง)

4. แก้ไข SocialProof (Shadcn UI Error)
   ใน app/landing/[template-id]/variants/starter/components/SocialProof.tsx:
   ปัญหา: Avatar ของ Shadcn ไม่ได้รับ src โดยตรง
   // ❌ ผิด: <Avatar src={review.avatar} />
   // ✅ ถูก:
   <Avatar>
   <AvatarImage src={review.avatar} alt={review.name} />
   <AvatarFallback>{review.name[0]}</AvatarFallback>
   </Avatar>

5. แก้ไข Services Icon (TS2305)
   ใน data/servicesData.ts ต้องทำการ export ฟังก์ชัน getServiceIcon:
   /\*_ @format _/
   import { MessageSquare, Banknote, ShieldCheck } from "lucide-react"

export const getServiceIcon = (iconName: string) => {
const icons: Record<string, any> = {
MessageSquare,
Banknote,
ShieldCheck
}
return icons[iconName] || MessageSquare
}

💡 ขั้นตอนสุดท้าย:

- Restart TS Server: ใน VS Code กด Ctrl+Shift+P เลือก TypeScript: Restart TS Server
- Verify Path: เช็คไฟล์ tsconfig.json ว่ามี "@/_": ["./_"] ใน paths หรือไม่
- Run Check: รัน pnpm type-check อีกครั้ง
  โครงสร้างของคุณตอนนี้มีความซับซ้อน (Complexity) สูง การรวมศูนย์ Type ไว้ที่ [template-id]/types/index.ts จะช่วยลด Error ได้ถาวรครับ พร้อมรันตรวจสอบใหม่ได้เลย!
